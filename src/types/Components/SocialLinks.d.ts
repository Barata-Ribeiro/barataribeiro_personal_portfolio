export interface SocialLink {
  url: string;
  icon: React.ReactElement;
  label: string;
}

export interface SocialButtonProps {
  href: string;
  title: string;
  icon: React.ReactElement;
  text: string;
}
